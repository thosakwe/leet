?? MAINFRAME(ARGS IS DATABASE OF STRING):
    INFILTRATE:
        FILE IS EXECUTE FILE_OPEN(ARGS[0])!
        CONTENTS IS EXECUTE FILE_READ(FILE)!
        HACK(CONTENTS)!
    ACCESS_DENIED:
        HACK("Couldn't find that file.")!